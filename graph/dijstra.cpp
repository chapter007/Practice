#include <stdio.h>
#include <vector>
#define N 1001
using namespace std;

struct E{
    int next,c;
    int cost;
};

vector<E> edge[N];
int dis[N];
int cost[N];
int main(){
    int n,m;
    while(scanf("%d %d",&n,&m)!=EOF){
        if(m==0&&n==0) break;
        while(m--){
            int a
        }
    }
}
